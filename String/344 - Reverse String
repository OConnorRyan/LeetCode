public class Solution {
    public void ReverseString(char[] s) {
        int end = s.Length - 1;
        for(int i = 0; i < (end / 2) + 1; i++)
        {
            char temp = s[i];
            s[i] = s[end - i];
            s[end - i] = temp;
            
        }
    }
}
